// ###Exercise 3 name: Orovwovo E. Oghale Matric no: CMP2307601#include <stdio.h> // Include standard input-output header fileint main() {    // Define the size of the array    int size = 5;    // Declare the array to store 5 elements    int array[size]; // Creating an array of integers with size 5    // Insert elements into the array    printf("Enter 5 elements:\n"); // Prompt the user to enter elements    for (int i = 0; i < size; i++) { // Loop to iterate over each element of the array        scanf("%d", &array[i]); // Read elements from the user and store them in the array    }    // Print the elements of the array    printf("Elements of the array:\n"); // Print a message indicating the following output will be the elements of the array    for (int i = 0; i < size; i++) { // Loop to iterate over each element of the array        printf("%d\n", array[i]); // Print each element of the array    }    return 0; // Return 0 to indicate successful program execution
